# Architecture

Food is independently deployed, built and stored. It has its own Next.js app, PostgreSQL database, Prisma schema, Android companion and environment configuration. It contains no CoffeeHQ package names or source imports.
